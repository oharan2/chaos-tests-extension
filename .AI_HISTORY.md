# AI History Log
This file tracks important changes and decisions in this directory and its sub-directories (when sensible).

## 2026-09-08
### MPEX AI Agent Instruction Files
- Added `.AI_INIT.md` (verbatim Best Practices `<prjRootDir>/.AI_INIT.md` block), `.AI_README.md`, and `.AI_HISTORY.md`.
- Added symbolic links `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `.cursorrules`, and `.continuerules` to `.AI_INIT.md`.
- Product standing constraints live in `.AI_README.md` (Common Settings / User Settings). `.cursor/` stays gitignored.
### Shell Scripts Aligned to Best Practices
- Updated `images/ote_wrapper.sh` and `hack/build-tests-ext.sh` to `set -euxo pipefail; shopt -s inherit_errexit`.
- Wrapper disables `xtrace` while reading ES/telemetry/AWS secret files (Krkn still requires those values as environment variables), uses `typeset` and PascalCase `function`, and selects the zone-outage node via JSONPath instead of text columns.
